{
  "name": "mealcoin-indexer",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "ts-node --transpile-only src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:rpc": "ts-node --transpile-only scripts/test-rpc.ts"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.x",
    "dotenv": "^16.x",
    "node-cron": "^4.2.1",
    "viem": "^2.x"
  },
  "devDependencies": {
    "@types/node": "^20.x",
    "@types/node-cron": "^3.0.11",
    "ts-node": "^10.x",
    "typescript": "^5.x",
    "vitest": "^2.1.9"
  }
}
